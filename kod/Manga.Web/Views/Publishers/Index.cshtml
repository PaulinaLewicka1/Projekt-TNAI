@model IEnumerable<Manga.Model.Entities.Publisher>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista</h1>

<p>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="CreatePublishers">Utwórz nowy</a>
    }
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Nazwa wydawnictwa
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="DetailsPublishers" asp-route-id="@item.Id">Szczegóły</a>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-action="EditPublishers" asp-route-id="@item.Id">Edytuj</a>
                        <a asp-action="DeletePublishers" asp-route-id="@item.Id">Usuń</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
